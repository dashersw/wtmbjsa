extends layout

block content
    h1 Log in
    form(action="/api/auth/local" method="post")
        div
            label Username:
            input(type="text" name="username")
        div
            label Password:
            input(type="password" name="password")
        div
            input(type="submit" value="Log in")
    a(href="/api/auth/twitter") Log in via twitter
